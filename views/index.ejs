<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Attendance Runs</title>
    <link rel="stylesheet" href="/assets/styles.css" />
  </head>
  <body>
    <main class="page">
      <section class="hero">
        <div>
          <p class="eyebrow">Discord Attendance Tracker</p>
          <h1>Whole-server tracking runs with per-channel CSV exports.</h1>
          <p class="intro">
            Start tracking from Discord, let the bot capture attendance across every voice channel,
            and use this dashboard to download one CSV per VC or a full export for the entire run.
          </p>
          <div class="actions">
            <a href="/" class="button-link">Runs</a>
            <a href="/users" class="button-link secondary-link">Users DB</a>
          </div>
        </div>
      </section>

      <section class="list">
        <% if (runs.length === 0) { %>
          <div class="empty-state">
            <h2>No tracking runs yet</h2>
            <p>Use <strong>/tracking start</strong> or <strong>/tracking schedule</strong> in Discord to create one.</p>
          </div>
        <% } %>

        <% runs.forEach((run) => { %>
          <article class="card">
            <div class="card-header">
              <div>
                <p class="eyebrow">Run #<%= run.id %></p>
                <h2><%= run.title %></h2>
              </div>
              <span class="status <%= run.is_active ? "live" : "idle" %>"><%= run.status %></span>
            </div>

            <dl class="meta">
              <div>
                <dt>Guild ID</dt>
                <dd><%= run.guild_id %></dd>
              </div>
              <div>
                <dt>Created</dt>
                <dd><%= run.created_display %></dd>
              </div>
              <div>
                <dt>Scheduled</dt>
                <dd><%= run.scheduled_start_display %> -> <%= run.scheduled_end_display %></dd>
              </div>
              <div>
                <dt>Actual Window</dt>
                <dd><%= run.started_display %> -> <%= run.ended_display %></dd>
              </div>
            </dl>

            <div class="actions">
              <a href="/runs/<%= run.id %>/export.csv" class="button-link">Download full CSV</a>
            </div>

            <div class="report">
              <h3>Tracked channels</h3>
              <% if (run.channels.length === 0) { %>
                <p class="empty-inline">No voice activity captured for this run.</p>
              <% } else { %>
                <% run.channels.forEach((channel) => { %>
                  <section class="subcard">
                    <div class="card-header">
                      <div>
                        <h3><%= channel.channel_name %></h3>
                        <p class="notes"><%= channel.participant_count %> participant(s)</p>
                      </div>
                      <a href="/runs/<%= run.id %>/channels/<%= channel.channel_id %>/export.csv" class="button-link">
                        Channel CSV
                      </a>
                    </div>

                    <table>
                      <thead>
                        <tr>
                          <th>User</th>
                          <th>Total time</th>
                          <th>Marked timings</th>
                        </tr>
                      </thead>
                      <tbody>
                        <% channel.report.forEach((entry) => { %>
                          <tr>
                            <td><strong><%= entry.username %></strong><br /><small><%= entry.user_id %></small></td>
                            <td><%= entry.total_display %></td>
                            <td><%= entry.sessions %></td>
                          </tr>
                        <% }) %>
                      </tbody>
                    </table>
                  </section>
                <% }) %>
              <% } %>
            </div>
          </article>
        <% }) %>
      </section>
    </main>
  </body>
</html>
